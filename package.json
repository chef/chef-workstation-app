{
  "name": "chef-workstation-app",
  "productName": "Chef Workstation App",
  "displayName": "Chef Workstation",
  "version": "0.1.6",
  "description": "Desktop application for Chef Workstation.",
  "repository": "https://github.com/chef/chef-workstation-tray",
  "author": {
    "name": "Chef Software, Inc.",
    "email": "workstation@chef.io",
    "url": "https://chef.sh"
  },
  "license": "Apache-2.0",
  "engines": {
    "node": "~10.0"
  },
  "scripts": {
    "clean": "rimraf build dist",
    "compile": "tsc && copyfiles -u 1 \"src/*.html\" build",
    "start": "npm run compile && electron ./build/App.js",
    "debug": "npm run compile && electron ./build/App.js --inspect-brk=9229",
    "build-all": "npm run compile && build --win --mac --linux --publish never",
    "build-mac": "npm run compile && build --mac --publish never",
    "build-linux": "npm run compile && build --linux --publish never",
    "build-win": "npm run compile && build --win --publish never",
    "test": "xvfb-maybe mocha test --full-stack"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "copyfiles": "^2.1.0",
    "electron": "^2.0.16",
    "electron-builder": "20.28.1",
    "electron-reload": "^1.2.5",
    "jsdom": "^12.1.0",
    "marked": "^0.5.1",
    "mocha": "^5.2.0",
    "rimraf": "^2.6.2",
    "spectron": "^4.0.0",
    "typescript": "^3.1.3",
    "xvfb-maybe": "^0.2.1"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.0",
    "electron-debug": "^2.0.0",
    "electron-is": "^3.0.0",
    "electron-osx-appearance": "^0.1.1",
    "request": "^2.0.0"
  }
}
